# 图测记录工具 — 接管记录 & 重建规划

> 用途：你接手这个（别人做的）工具后的速查与决策依据。
> 配合 `viso test1.md`（原作者的功能/设计备忘录）一起看：那份讲「有哪些功能、怎么实现」，本份讲「怎么部署、谁能访问、接下来怎么改」。
> 建档日期：2026-06-29

---

## 一、这是什么（一句话）

一个 **Google Play A/B 图测 / 直接更新** 的记录管理工具。纯前端单页应用，**没有后端、没有数据库服务、没有登录系统**。数据存在群晖（Synology）网络盘的 JSON 文件里，靠浏览器的 File System Access API 直接读写。

当前你看到的访问地址：`http://192.168.211.90:8502/`（局域网内某台机器上起的一个静态文件服务，**这个服务本身不在本仓库里**，仓库里只有 `public/` 静态资源 + 文档）。

---

## 二、部署现状（实际怎么跑起来的）

| 项 | 现状 |
|---|---|
| 代码形态 | 纯静态：`public/index.html`（CSS+JS 全部 inline，构建产物）+ 源文件 `css/`、`js/` |
| 后端 | **无**。没有任何服务端代码（已确认仓库内无 .py/Dockerfile/server.js 等） |
| 服务器 | 局域网某机器上一个静态服务器，监听 `:8502`，不在仓库内，重启/迁移要靠原作者环境 |
| 数据存储 | 群晖网络盘上的 `data/` 文件夹（JSON 文件），需在 Windows 映射盘符（如 `Z:\图测记录工具\data`），不支持 UNC 路径 |
| 浏览器要求 | 必须 Chrome / Edge（依赖 File System Access API） |
| 外部依赖 | 仅 Chart.js + Tesseract.js（CDN 引入） |

### 构建流程（改完源码要重新打包）

源码改的是 `public/css/styles.css` 和 `public/js/*.js`，但页面真正加载的是打包后的 `public/index.html`。原作者用两个临时脚本打包：

```bash
python3 /tmp/strip_imports.py   # 去掉 import/export → bundle.js
python3 /tmp/rebuild_html.py    # 把 CSS+JS inline 进 index.html
```

> ⚠️ 这两个打包脚本在 `/tmp` 下、**没进仓库**，重装环境就丢了。优化前第一件事建议：把打包脚本补进仓库（见第六节）。

---

## 三、数据 & 访问模型（最关键，决定「只有我能用」能不能实现）

### 数据怎么存

- 每个用户在自己浏览器里通过「选择文件夹」授权访问群晖上的 `data/` 文件夹。
- 浏览器把文件夹 handle 存在 IndexedDB（`chart-recorder` 库），下次可「恢复访问」。
- 多人协作 = 所有人都把浏览器指向**同一个**群晖文件夹；写入用「先读最新 JSON → 合并 → 再写」策略防覆盖。
- 断网/盘掉线时写入 `pending` 队列，恢复后重试。

### ⚠️ 访问控制现状：**完全没有**

- 工具**没有任何登录、密码、用户体系**。`index.html` 里的 `.login-page` / `.login-card` 只是「选择文件夹」和「浏览器不支持」两个引导页的样式，不是真正的鉴权。
- 谁能打开 `http://192.168.211.90:8502/` + 谁能访问那个群晖文件夹，谁就能用全部数据。
- 「权限」只有一层：群晖网络盘本身的盘符/账号权限。Web 层面零防护。

> 结论：**现在这个工具天然做不到「只有我能用」**。要做私有版，必须新增访问控制层（见第五节）。

### File System Access API 的隐藏坑（可能就是你说的「打不开 / 解锁」）

`window.showDirectoryPicker` 只在「安全上下文」可用：`https://` 或 `localhost`。
**纯 `http://` + 局域网 IP（正是 `http://192.168.211.90:8502/`）不是安全上下文** → 该 API 为 `undefined` → 页面会卡在「浏览器不支持」或无法选择文件夹。

可能的现象/对策：
- 在起服务那台机器本机用 `http://localhost:8502/` 打开 → 正常（localhost 算安全上下文）。
- 用局域网 IP 在别的机器打开 → 大概率用不了文件夹功能。
- 彻底解决：给服务上 HTTPS（哪怕自签证书），或改用不依赖该 API 的存储方案（重建版推荐，见第五节）。

---

## 四、代码地图（改代码时从哪下手）

| 文件 | 职责 | 关键点 |
|---|---|---|
| `public/index.html` | 打包产物，浏览器实际加载的就是它 | 入口自执行 `(async()=>{…})()` 在 ~1720 行：判断 API 支持 → `isReady()` → 进主程序 or 渲染文件夹选择页 |
| `public/js/app.js` | 主逻辑（~2150 行）：路由、表单、时间线、仪表盘、OCR、裁剪、灯箱等 | 全局函数靠文件末尾 `Object.assign(window,{…})` 暴露（因为不能用 ES 模块） |
| `public/js/db.js` | 存储层（~410 行）：IndexedDB + 文件夹读写 + 待同步队列 + 备份/回收站 | `pickFolder` / `resumeAccess` / `isReady` / `verifyPermission`；读写走 `readJSON`/`writeJSON` |
| `public/js/effects.js` | 效果计算（CI + 安装数 → 很好/不错/持平/很差…）| `effectBadgeHTML` |
| `public/js/config.js` | 已废弃的旧 Firebase 配置占位，现无实际作用 | — |
| `public/css/styles.css` | 样式源文件（~1085 行）| 改样式改这里，再重新打包 |

功能模块明细见 `viso test1.md` 第三~十一节（表单/OCR/裁剪/时间线/仪表盘/数据结构/设计系统/流量预设/实验类型）。

---

## 五、全新私有版本规划（「换个链接、只有我能用」）

目标：独立链接、独立部署、**只有你能访问**，不依赖原作者那台机器和那个群晖盘的 File System Access API。

### 推荐架构（按省心程度排序）

**方案 A — 托管静态 + 后端持久化（最推荐，能真正私有）**
- 前端：把现有纯前端搬到 Vercel / Cloudflare Pages / Netlify（自动 HTTPS，解决安全上下文坑）。
- 鉴权：套一层访问控制——
  - 最简单：Cloudflare Access / Vercel 密码保护（一个邮箱白名单或一个密码，零代码）。
  - 进阶：加个轻后端（Cloudflare Workers / 一个小 Node 服务）做登录。
- 数据：从「浏览器直接读 NAS 文件」改为「后端存储」——Cloudflare KV/D1、Supabase、或一个挂卷的小数据库。彻底摆脱 File System Access API 的限制。

**方案 B — 保留 NAS，但加 HTTPS + 密码**
- 继续放群晖，用群晖自带的 反向代理 + Let's Encrypt 证书做 HTTPS（解决 API 安全上下文）。
- 用群晖反代或前置一层 Basic Auth / 群晖账号登录限制访问。
- 数据仍走 File System Access API + NAS 文件夹。改动小，但仍受「必须 Chrome/Edge + 每次授权文件夹」的体验约束。

**方案 C — 完全本地单机版**
- 只在你自己电脑 `localhost` 跑，不对局域网开放。
- 零鉴权也安全（别人访问不到），但就只有你这台机器能用，换设备就没了。

### 建议
- 如果你要「随时随地、只有我能用、体验好」→ **方案 A**（静态托管 + Cloudflare Access + 云端存储）。
- 如果你想最小改动、数据继续留在公司群晖 → **方案 B**。

> 真正动手做新版时，需要先定三件事：①部署在哪（云 / 群晖 / 本机）②鉴权方式（密码 / 邮箱白名单 / 账号）③数据存哪（云数据库 / 继续 NAS 文件）。定了我再据此搭。

---

## 六、接管后建议的「先做项」（优化前的地基）

1. **把打包脚本入库**：现在 `strip_imports.py` / `rebuild_html.py` 在 `/tmp`，丢了就没法改源码重打包。补进仓库（如 `scripts/`）并在本文件记录用法。
2. **确认源码 ↔ 产物一致**：`public/js/*.js` 是否就是 `index.html` 里 inline 的那版？避免改了源码忘了同步进产物。
3. **决定改造路线**：是「继续优化现有版」还是「直接做私有新版」——两者地基不同，早定早省事。
4. **备份现有 NAS 数据**：动任何代码前，先把群晖 `data/` 整份拷出来。

---

## 七、待优化方向（占位，你后续补充）

- [ ] （在此记录你想改的点）
